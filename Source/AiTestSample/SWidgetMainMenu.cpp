// Fill out your copyright notice in the Description page of Project Settings.


#include "SWidgetMainMenu.h"

#define LOCTEXT_NAMESPACE "MainMenu"

void SWidgetMainMenu::Construct(const FArguments& InArgs)
{
	/*const FMargin ContentPadding = FMargin(500.f, 300.f);
	const FText TitleText = LOCTEXT("GameTitle", "My SUper Create Game");

	ChildSlot
		[
			SNew(SOverlay)
				+ SOverlay::Slot()
				.HAlign(HAlign_Fill)
				.valign(VAlign_Fill)
				[
					SNew(SImage)
					.ColorAndOpacity(FColor::Black)
				]
				+ SOverlay::Slot()
				.HAlign(HAlign_Fill)
				.valign(VAlign_Fill)
				.Padding(ContentPadding)
				[
					SNew(SVerticalBox)
						+ SVerticalBox::Slot()
						[
							SNew(STextBlock)
								.Text(TitleText)
						]
				]
		];*/
}
#undef LOCTEXT_NAMESPACE